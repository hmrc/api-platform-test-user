{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "description": "Create Individual Response",
  "type": "object",
  "properties": {
    "userId": {
      "type": "string",
      "description": "Government Gateway ID for the generated user"
    },
    "password": {
      "type": "string",
      "description": "Government Gateway password for the generated user"
    },
    "saUtr": {
      "type": "string",
      "description": "Enrols a user for Self Assessment and generates a SA UTR"
    },
    "nino": {
      "type": "string",
      "description": "Enrols a user for National Insurance and generates a National Insurance Number"
    },
    "mtdItId": {
      "type": "string",
      "description": "Enrols a user for Making Tax Digital Income Tax and generates a MTD IT ID"
    }
  },
  "required": ["userId", "password"]
}
